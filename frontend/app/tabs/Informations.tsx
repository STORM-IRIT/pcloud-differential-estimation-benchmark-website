"use client";

import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "../components/ui/card";
import { Separator } from "../components/ui/separator";

// const references = [
//     {
//       id: 1,
//       text: "Mellado N., Marcadet Q., Espinasse L., Mora P., Dutailly B., Tournon-Valiente S., Granier X.: 3D-ARD: A 3d-acquired research dataset, June 2020.",
//     },
//   ];
const references: any[] = [];

  const ref = (id: number) => (
    <sup>
      <a
        href={`#ref-${id}`}
        className="text-blue-600 hover:underline"
        title={`See reference ${id}`}
      >
        [{id}]
      </a>
    </sup>
  );

const teaser_caption = (
    <>
      Computing differential properties on large point clouds (left) is a classical task in many geometry processing pipelines. 
      <br />
      A lot of related works has been proposed to locally estimate such quantities, as illustrated here with a visual comparison of mean curvature (<span className="overline">H</span>) estimators 
      (values are clamped to the [-40,40], unsigned/signed curvature values for the top/bottom row respectively).
      <br />
    </>
);

const Informations: React.FC = () => {
  const teaser = {
    url: "/Teaser.png",
    caption: teaser_caption,
  };

  const title = "Survey on differential estimators for 3d point clouds";

//   const authors = [
//     { name: "Nom1", affiliationIndex: [1] },
//     { name: "Nom2", affiliationIndex: [1, 2] },
//     { name: "Nom3", affiliationIndex: [2] },
//   ]; 
 
// const affiliations = [
//   { index: 1, name: "UNIVERSITY, COUNTRY" },
//   { index: 2, name: "LABO 1" },
// ];
  const authors = [
    { name: "Submission id: 1001", affiliationIndex: [] },
  ]; 

  const affiliations: any[] = [
    // { index: 1, name: "" },
  ];

const abstract = (
    <>
        Recent advancements in 3D scanning technologies, including LiDAR and photogrammetry, have enabled the precise digital replication of real-world objects. These methods are widely used in fields such as GIS, robotics, and cultural heritage. However, the point clouds generated by such scans are often noisy and unstructured, posing challenges for traditional geometry processing. Accurately estimating differential properties like surface curvatures and normals is crucial for tasks such as shape matching and classification, but remains complex due to these inherent challenges.
            <br />
        This paper reviews state-of-the-art methods for estimating differential properties from 3D point clouds, with a focus on approaches that offer strong mathematical foundations and theoretical guarantees. We also benchmark these methods using various datasets, evaluating their performance in terms of accuracy, robustness, and efficiency. Our contributions include the release of datasets, tools, and code to promote reproducibility and support future research in this area.
    </>
);

  return (
    <div className="flex flex-col space-y-6 w-full max-w-5xl mx-auto p-6">
      {teaser.url && (
        <figure className="w-full">
          <img
            src={teaser.url}
            alt="Teaser"
            className="rounded-2xl w-full object-cover shadow-md"
          />
          {teaser.caption && (
            <figcaption className="text-left text-sm text-gray-500 mt-2 italic">
              <span className="underline">Caption:</span> {teaser.caption}
            </figcaption>
          )}
        </figure>
      )}

      <h1 className="text-3xl font-bold text-center">{title}</h1>

      <div className="text-center text-gray-250 space-x-2">
        {authors.map((author, idx) => (
          <span key={idx}>
            {author.name}
            {author.affiliationIndex.map((i, j) => (
              <sup key={j} className="text-xs align-super">
                {i}
              </sup>
            ))}
            {idx < authors.length - 1 && ", "}
          </span>
        ))}
      </div>

      <div className="text-center text-sm text-gray-600">
        {affiliations.map((aff, idx) => (
          <div key={idx}>
            <sup>{aff.index}</sup> {aff.name}
          </div>
        ))}
      </div>

      <Separator />

      <Card className="shadow-sm border border-gray-200">
        <CardHeader>
          <CardTitle>Abstract</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-justify leading-relaxed text-gray-350">{abstract}</p>
        </CardContent>
      </Card>

      {references.length > 0 && (
        <>
          <Separator />
          <section>
            <h2 className="text-2xl font-semibold mb-4">References</h2>
            <ol className="list-decimal ml-6 space-y-1 text-gray-700">
              {/* If no reference, display nothing */}
              {references.length != 0 && references.map((r) => (
                <li key={r.id} id={`ref-${r.id}`}>
                  {r.text}
                </li>
              ))}
            </ol>
          </section>
        </>
      )}
    </div>
  );
};

export default Informations;

export { Informations };